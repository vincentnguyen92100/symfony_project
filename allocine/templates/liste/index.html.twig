{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-2">
            <div class="list-group">
            <a href="{{ path('new_list') }}" class="list-group-item list-group-item-action">CREATE A LIST</a>
               
            </div>
        </div>
        <div class="col-10">
<h1>YOUR LIST(S)</h1>           
            <hr>
            <div class="row">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
 
                <th>Name</th>
                <th>description</th>
            </tr>
        </thead>
        <tbody>
        {% for liste in listes %}
            <tr>
                <td>{{ liste.id }}</td>

                <td>{{ liste.name }}</td>
                <td>{{ liste.description }}</td>
                <td>
                    {# <a href="{{ path('liste_show', {'id': liste.id}) }}">show</a>
                    <a href="{{ path('liste_edit', {'id': liste.id}) }}">edit</a> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
   
    {# <a class="btn btn-info" href="{{ path('users_new') }}">Create new</a> #}

            </div>
        </div>
    </div>



{% endblock %}
